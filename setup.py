from setuptools import setup

setup(name='glint_server',
        license='MIT',
        url='https://github.com/peteraw77/glint_server',
        packages=['glint_server'],
        zip_safe=True,
        install_requires=[
            'spacy',
        ]
        )
